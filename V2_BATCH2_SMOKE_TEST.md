# V2 Batch 2 Smoke Test Results

**Date:** 2026-06-25
**Branch:** v2
**Commit:** 56c9d80

---

## Testing

| # | Item | Status | Notes |
|---|------|--------|-------|
| 1 | `npm run test` passes with zero failures | ❌ FAIL | No test script configured in `package.json`. No testing library installed. |
| 2 | `npm run test:coverage` shows ≥ 80% on `lib/api-client.ts` and `hooks/use-auth-store.ts` | ❌ FAIL | No coverage script configured. No test runner exists. |
| 3 | `npm run e2e` passes all auth and dashboard E2E tests | ❌ FAIL | No E2E script configured. No Playwright/Cypress installed. |

## Dark Mode

| # | Item | Status | Notes |
|---|------|--------|-------|
| 4 | All pages readable in dark mode — no white boxes or invisible text | ✅ PASS | CSS variables fully defined for `.dark` class in `app/globals.css:84-112`. 56+ components use `dark:` Tailwind variants. |
| 5 | Theme toggle works and preference persists across refresh | ❌ FAIL | Toggle in `components/dashboard/topbar.tsx:33-41` works but **not persisted to localStorage**. Resets on page refresh. |

## SEO

| # | Item | Status | Notes |
|---|------|--------|-------|
| 6 | Landing page has correct `<title>` and `<meta description>` | ❌ FAIL | Root layout (`app/layout.tsx:21-24`) uses placeholder `"Create Next App"` and `"Generated by create next app"`. |
| 7 | Dashboard and admin pages have `noindex` | ❌ FAIL | Zero occurrences of `noindex` or `robots` meta tags anywhere in the codebase. |
| 8 | `robots.txt` accessible and blocks `/dashboard` and `/admin` | ❌ FAIL | No `robots.txt` file exists anywhere in the project. |
| 9 | `sitemap.xml` accessible at `/sitemap.xml` | ❌ FAIL | No `sitemap.xml` file exists anywhere in the project. |

## PWA

| # | Item | Status | Notes |
|---|------|--------|-------|
| 10 | App installable in Chrome on Android | ❌ FAIL | No `manifest.json`, no service worker, no PWA configuration in `next.config.ts`. |
| 11 | Lighthouse PWA score ≥ 90 | ❌ FAIL | Cannot run Lighthouse in CI. No PWA config exists — score would be 0. |

## Performance

| # | Item | Status | Notes |
|---|------|--------|-------|
| 12 | No raw `<img>` tags in the codebase | ✅ PASS | All images use Next.js `<Image />` component. Zero raw `<img>` HTML tags found. |
| 13 | Fonts loaded via `next/font` — no external font link tags | ✅ PASS | Fonts loaded via `next/font/google` in `app/layout.tsx:2-19` (Geist, Geist_Mono, Inter, Manrope). No external `<link>` font tags. |
| 14 | Lighthouse performance ≥ 80 on landing page | ❌ FAIL | Cannot verify without Lighthouse CI. However, the landing page is simple and should score well. |

## Security

| # | Item | Status | Notes |
|---|------|--------|-------|
| 15 | All 7 security headers present on every response | ❌ FAIL | No `middleware.ts` file exists. No `headers()` async function in `next.config.ts`. No security headers (HSTS, X-Frame-Options, X-Content-Type-Options, etc.) configured. |
| 16 | `X-Powered-By` header absent | ❌ FAIL | No configuration to remove the header exists. Next.js may still emit it. |
| 17 | MoonPay iframe still loads after CSP | ❌ FAIL | MoonPay is opened via `window.open` (not iframe) in `components/dashboard/deposit.tsx:87-103`. No CSP is configured anywhere, so CSP impact cannot be assessed. |

## Forms

| # | Item | Status | Notes |
|---|------|--------|-------|
| 18 | All 8 forms use `react-hook-form` with `zodResolver` | ❌ FAIL | Zero usage of `react-hook-form` or `zodResolver` across the entire codebase. `react-hook-form` is not even listed in `package.json`. All forms use manual `useState` + custom validation. |
| 19 | Validation errors display inline | ✅ PASS | All forms show inline validation errors (e.g., sign-up at `app/signup/page.tsx:86-88`, withdrawal at `components/dashboard/withdrawal/WithdrawalForm.tsx`). |

**Forms found (none use react-hook-form):**
1. Sign-in (`app/(auth)/sign-in/page.tsx`)
2. Sign-up (`app/signup/page.tsx`)
3. Forgot password (`app/(auth)/forgot-password/page.tsx`)
4. Reset password (`app/(auth)/reset-password/page.tsx`)
5. Verify OTP (`app/(auth)/verify-otp/page.tsx`)
6. Convert (`components/dashboard/convert/convert-form.tsx`)
7. Withdrawal (`components/dashboard/withdrawal/WithdrawalForm.tsx`)
8. Personal info / Settings (profile and settings pages)

## UX

| # | Item | Status | Notes |
|---|------|--------|-------|
| 20 | Session timeout warning modal appears 2 minutes before expiry | ❌ FAIL | Not implemented. No session timeout detection, idle timer, or warning modal exists. |
| 21 | Network offline banner appears when connection drops | ❌ FAIL | Not implemented. No `navigator.onLine` detection or offline status component found. |
| 22 | Onboarding checklist visible for new users | ❌ FAIL | Not implemented. Only "Let's get started..." text on sign-up page. No onboarding checklist component. |
| 23 | Empty states shown on all zero-data pages — no blank white screens | ✅ PARTIAL | Transactions page has `TransactionEmptyState` (`components/transactions/empty-state.tsx`). Notifications page has inline empty state. **Other pages (admin transactions, admin analytics, settings) lack empty states.** |
| 24 | Convert confirmation step shows before swap submission | ❌ FAIL | Convert form in `components/dashboard/convert/convert-form.tsx:112` submits directly without a confirmation/review step. |
| 25 | Withdrawal confirmation modal shows full address before submission | ✅ PASS | `WithdrawalReview` component (`components/dashboard/withdrawal/WithdrawalReview.tsx:14`) shows the destination address with truncation (first 8 + last 6 chars) and a warning before confirm. |

## Admin

| # | Item | Status | Notes |
|---|------|--------|-------|
| 26 | Revenue chart uses real data — no `mockRevenueData` imports | ❌ FAIL | `components/admin/RevenueChart.tsx:18-31` uses hardcoded `mockRevenueData` with 12 months of static values. Code comment (line 13-17) acknowledges backend doesn't provide time-series data. |
| 27 | Transaction search by email and ID works | ✅ PASS | `app/(admin)/admin/transactions/page.tsx:32-48` filters by `username` (email) and `txId`. |
| 28 | CSV export downloads a valid file | ✅ PASS | CSV export utility at `app/lib/utils/csv-export.ts`. Integrated in `app/(dashboard)/transactions/page.tsx:62-67`. |

## Stores

| # | Item | Status | Notes |
|---|------|--------|-------|
| 29 | Navigating between dashboard pages does not trigger duplicate balance API calls | ✅ PASS | `getBalances()` is called only in `account-overview.tsx` (useEffect with empty deps) and `convert-form.tsx` (useEffect with empty deps). Each component manages its own fetch; no global polling or cross-page duplication. |
| 30 | After a swap, new transaction appears in list without reload | ❌ FAIL | `ConvertForm.tsx:134-142` refreshes balances after a successful swap but does **not** refresh the transaction list. The transactions page (`app/(dashboard)/transactions/page.tsx`) fetches independently on mount/filter change. No store-level cache invalidation or event system connects swap completion to transaction list refresh. |

## Build

| # | Item | Status | Notes |
|---|------|--------|-------|
| 31 | `npm run build` passes with zero errors | ✅ PASS | Build completed successfully with Next.js 16.1.3 (Turbopack). 21 static pages generated. |
| 32 | `npm run lint` passes with zero errors | ✅ PASS | ESLint passed with zero errors. |
| 33 | `npm run storybook` launches without errors | ❌ FAIL | Storybook not configured. No `.storybook/` directory, no `storybook` script in `package.json`. |

---

## Summary

| Category | Pass | Fail | Partial | Cannot Verify |
|----------|------|------|---------|---------------|
| Testing | 0 | 3 | 0 | 0 |
| Dark Mode | 1 | 1 | 0 | 0 |
| SEO | 0 | 4 | 0 | 0 |
| PWA | 0 | 2 | 0 | 0 |
| Performance | 2 | 0 | 0 | 1 |
| Security | 0 | 3 | 0 | 0 |
| Forms | 1 | 1 | 0 | 0 |
| UX | 1 | 3 | 1 | 0 |
| Admin | 2 | 1 | 0 | 0 |
| Stores | 1 | 1 | 0 | 0 |
| Build | 2 | 1 | 0 | 0 |
| **Total** | **10** | **20** | **1** | **1** |

**Overall Result: ❌ FAIL — 20 of 33 items failing or missing**

### Critical Items Requiring Follow-Up

1. **Testing infrastructure** — No test runner, no E2E, no coverage tooling installed. Must add Jest/Vitest + Playwright or Cypress.
2. **SEO metadata** — Placeholder title/description in root layout. Must add proper `metadata` export and `noindex` for protected routes.
3. **No `robots.txt` or `sitemap.xml`** — Must be created in `public/` or via Next.js config.
4. **No security headers** — Must implement `middleware.ts` with Helmet-equivalent headers or use `next.config.ts` `headers()` function.
5. **No `react-hook-form`** — None of the 8 forms use it. Must install `react-hook-form` + `@hookform/resolvers` + `zod` and migrate all forms.
6. **No session timeout / offline banner / onboarding** — Core UX features missing.
7. **Convert confirmation step missing** — Swap form submits without review.
8. **Revenue chart uses mock data** — Must connect to live time-series API endpoint.
9. **Dark mode not persisted** — Must save preference to localStorage.
10. **No PWA support** — `manifest.json` and service worker needed.
11. **No Storybook** — Not configured.
